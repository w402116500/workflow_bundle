# Design

The system uses a Vue frontend, a Spring Boot backend, MySQL for off-chain business data, and Hyperledger Fabric for trace records.

Fabric deployment uses peer, orderer, channel, and chaincode lifecycle scripts.
